# 📂 ascii-scaffolder

**Turn ASCII directory trees into real files and folders instantly.**

LLMs (like ChatGPT, Claude, or DeepSeek) often output project structures as ASCII trees. Creating them manually is tedious. `ascii-scaffolder` automates this in seconds.

## Usage

1. **Run the script:**
   ```bash
   python scaffold_tree.py
    ```
2. **Copy and paste your ASCII tree**

3. **Enjoy the time saved**